
const initialState = {
    data: []
};

function accountsReducer (state = initialState, action) {
    switch (action.type) {
        
        default :
            return state;
    }
}

export default accountsReducer;